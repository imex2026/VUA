# Copy to .env and fill in. Secrets live here, never in config.yaml.

# Required: the brain runs on Claude.
ANTHROPIC_API_KEY=

# Optional cloud fallbacks (Phase 2+). Leave empty to stay fully local.
# ELEVENLABS_API_KEY=
# OPENAI_API_KEY=
# BRAVE_SEARCH_API_KEY=
